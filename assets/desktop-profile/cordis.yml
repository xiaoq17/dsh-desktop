# dsh profile root for the desktop profile — an empty entry list. The tree is
# composed as patches: each bundle in package.json's dsh.profile.bundles, then
# cordis.patch.yml, then any --patch overlays. Edit cordis.patch.yml, not this file.
[]
